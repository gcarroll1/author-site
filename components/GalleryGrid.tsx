export default function GalleryGrid({ items }:{ items:{src:string;alt?:string;caption?:string}[]}){return(<div className="grid grid-cols-2 md:grid-cols-3 gap-3">{items.map((it,i)=>(<figure key={i} className="overflow-hidden rounded-xl border border-neutral-800 bg-neutral-900"><a href={it.src} target="_blank" rel="noopener noreferrer"><img src={it.src} alt={it.alt||''} className="w-full h-40 object-cover" /></a>{it.caption&&<figcaption className="text-xs text-neutral-400 p-2">{it.caption}</figcaption>}</figure>))}</div>);}